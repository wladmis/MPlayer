diff -urN mplayer-svn-r22590.orig/libdha/Makefile mplayer-svn-r22590/libdha/Makefile
--- mplayer-svn-r22590.orig/libdha/Makefile	2007-03-15 02:36:39 +0200
+++ mplayer-svn-r22590/libdha/Makefile	2007-03-15 02:42:07 +0200
@@ -11,12 +11,8 @@
 SHORTNAME = libdha.so.$(MAJOR_VERSION)
 VSHORTNAME = libdha.so
 endif
-ifeq ($(TARGET_WIN32),yes)
 LIBNAME = libdha.a
 SHORTNAME = libdha.a
-else
-LIBNAME = libdha.so.$(VERSION)
-endif
 
 SRCS=libdha.c \
      mtrr.c \
@@ -43,13 +39,7 @@
 all: pci_names.c $(LIBNAME) $(SHORTNAME)
 
 $(LIBNAME):     $(OBJS)
-ifeq ($(TARGET_WIN32),yes)
 	$(AR) r $@ $^
-else
-	$(CC) -shared -Wl,-soname -Wl,$@ -o $@ $^ $(LIBS)
-	ln -sf $@ $(SHORTNAME)
-	ln -sf $@ $(VSHORTNAME)
-endif
 
 pci_names.c:	oth/pci.db
 	LC_ALL=C $(AWK) -f pci_db2c.awk $<
@@ -70,8 +60,11 @@
 install:
 	-mkdir -p $(LIBDIR)
 	$(INSTALL) -m 755 $(INSTALLSTRIP) -p $(LIBNAME) $(LIBDIR)/$(LIBNAME)
+ifneq ($(LIBNAME),$(SHORTNAME))
 	ln -sf $(LIBNAME) $(LIBDIR)/$(SHORTNAME)
+else
 	-$(LDCONFIG)
+endif
 
 uninstall:
 	rm -f $(LIBDIR)/libdha.so $(LIBDIR)/$(SHORTNAME) $(LIBDIR)/$(LIBNAME)
diff -urN mplayer-svn-r22590.orig/Makefile mplayer-svn-r22590/Makefile
--- mplayer-svn-r22590.orig/Makefile	2007-03-15 01:20:31 +0200
+++ mplayer-svn-r22590/Makefile	2007-03-15 02:39:38 +0200
@@ -207,10 +207,10 @@
 tremor/libvorbisidec.a:
 	$(MAKE) -C tremor
 
-libdha/libdha.so:
+libdha/libdha.a:
 	$(MAKE) -C libdha
 
-vidix/libvidix.a: libdha/libdha.so
+vidix/libvidix.a: libdha/libdha.a
 	$(MAKE) -C vidix
 
 Gui/libgui.a:
@@ -297,7 +297,6 @@
 	$(INSTALL) -m 644 etc/mplayer.desktop $(prefix)/share/applications/
 
 install-vidix:
-	$(MAKE) -C libdha install
 	mkdir -p $(LIBDIR)/mplayer/vidix
 	$(INSTALL) -m 755 $(INSTALLSTRIP) -p vidix/drivers/*.so $(LIBDIR)/mplayer/vidix
 
@@ -417,7 +416,7 @@
 libfaad2/libfaad2.a: .norecurse $(wildcard libfaad2/*.[ch] libfaad2/*/*.[ch])
 
 loader/libloader.a: .norecurse $(wildcard loader/*.[chSs])
-libdha/libdha.so: .norecurse $(wildcard libdha/*.[ch])
+libdha/libdha.a: .norecurse $(wildcard libdha/*.[ch])
 vidix/libvidix.a: .norecurse $(wildcard vidix/*.[ch])
 Gui/libgui.a: .norecurse $(wildcard Gui/*.[ch] Gui/*/*.[ch] Gui/*/*/*.[ch])
 
