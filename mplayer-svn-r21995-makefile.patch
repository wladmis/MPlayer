diff -urN mplayer-svn-r21995.orig/Makefile mplayer-svn-r21995/Makefile
--- mplayer-svn-r21995.orig/Makefile	2007-01-22 19:01:55 +0200
+++ mplayer-svn-r21995/Makefile	2007-01-24 02:39:11 +0200
@@ -256,7 +256,9 @@
 	-install -m 755 $(INSTALLSTRIP) -p vidix/drivers/*.so $(LIBDIR)/mplayer/vidix
 endif
 	$(INSTALL) -d $(BINDIR)
+ifeq ($(MPLAYER),yes)
 	$(INSTALL) -m 755 $(INSTALLSTRIP) mplayer$(EXESUF) $(BINDIR)
+endif
 	$(INSTALL) -d $(MANDIR)/man1
 	for i in $(MAN_LANG); do \
 		if test "$$i" = en ; then \
@@ -277,6 +279,7 @@
 	done
 endif
 	$(INSTALL) -d $(DATADIR)
+ifeq ($(MPLAYER),yes)
 	$(INSTALL) -d $(DATADIR)/font
 ifeq ($(GUI),yes)
 	-ln -sf mplayer$(EXESUF) $(BINDIR)/gmplayer$(EXESUF)
@@ -288,6 +291,7 @@
 	$(INSTALL) -d $(prefix)/share/applications
 	$(INSTALL) -m 644 etc/mplayer.desktop $(prefix)/share/applications/
 endif
+endif
 	$(INSTALL) -d $(CONFDIR)
 	if test -f $(CONFDIR)/codecs.conf ; then mv -f $(CONFDIR)/codecs.conf $(CONFDIR)/codecs.conf.old ; fi
 
